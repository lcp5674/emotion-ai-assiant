# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
pymysql==1.1.0
alembic==1.13.1
redis==5.0.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-dotenv==1.0.0

# Pydantic
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0.post1

# LLM Providers
openai==1.10.0
anthropic==0.18.0
qianfan>=0.3.4  # 百度文心

# Sentiment Analysis (CPU only)
# snownlp - 轻量级中文情感分析库，无需深度学习框架
snownlp>=0.12.0

# 如果需要 transformers 模型（更大但更准确），使用 CPU 版本
# torch>=2.0.0+cpu; extra == 'cpu'  # CPU版本需要从源码安装，太复杂

# HTTP client
httpx==0.26.0

# Utils
requests==2.31.0
python-dateutil==2.8.2
aiofiles==23.2.1

# Security
bleach==6.1.0
cryptography==42.0.5

# Logging
loguru==0.7.2

# CORS
starlette==0.35.1

# Testing (dev only)
pytest==8.0.0
pytest-asyncio==0.23.3
freezegun==1.4.0
